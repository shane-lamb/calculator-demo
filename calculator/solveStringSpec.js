var sinon = require('sinon');
var should = require('chai').should();
var expect = require('chai').expect;

var solveString = require('./solveString');

describe("something", function () {
    it("does something", function () {

    });
});